
  <% if obj.errors.any? %>
    <div class="card red darken-1" id="error_explanation">
      <div class="card-content white-text">
      <h6><%= pluralize(obj.errors.count, "error") %> prohibited this user from being saved:</h6>

      <ul>
        <% obj.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
      </div>
    </div>
  <% end %>
